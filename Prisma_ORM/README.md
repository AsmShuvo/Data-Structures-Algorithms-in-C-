# Prisma ORM - ইন্টারভিউ নোটস (English-Bangla Mixed) 🚀

## ১. Prisma কি? (The "Elevator Pitch")

Prisma হলো Node.js এবং TypeScript-এর জন্য একটি **নেক্সট-জেনারেশন Object-Relational Mapper (ORM)**। সাধারণ ORM-এর মতো না হয়ে, এটি একটি কাস্টম `schema.prisma` ফাইল ব্যবহার করে ডেটা মডেল ডিফাইন করে এবং অটোমেটিক একটি **type-safe** ডেটাবেস ক্লায়েন্ট জেনারেট করে।

- **মূল পার্থক্য:** এটি **Type Safety** এবং **Developer Experience (DX)** (অটো-কমপ্লিশন, সহজ API)-এর ওপর বেশি ফোকাস করে।

---

## ২. মূল উপাদানসমূহ (Core Components)

ইন্টারভিউতে এই তিনটি কম্পোনেন্টের কথা অবশ্যই বলবেন:

1.  **Prisma Client**: এটি হলো অটো-জেনারেটেড কুয়েরি বিল্ডার যা টাইপ-সেফ। আমরা কোড-এ এটিই ব্যবহার করি (যেমন `prisma.user.findMany()`)।
2.  **Prisma Migrate**: এটি একটি মাইগ্রেশন সিস্টেম। আপনি `schema.prisma` ফাইলে চেঞ্জ করবেন, আর এটি অটোমেটিকালি ডেটাবেসে সেই অনুযায়ী টেবিল আপডেট করে দেবে SQL চালিয়ে।
3.  **Prisma Studio**: ডেটাবেসের ডেটা দেখার এবং এডিট করার জন্য একটি GUI (ভিজ্যুয়াল টুল)।

---

## ৩. কেন Prisma ব্যবহার করবেন? (Pros)

- **Type Safety**: যদি ডেটাবেসে `User` মডেলে `email` ফিল্ড থাকে, TypeScript সেটা চিনে ফেলবে। `user.emial` লিখলে ভুল ধরে দেবে।
- **Auto-completion**: কোড করার সময় সাজেশন দেখায়, তাই ভুল কম হয়।
- **Single Source of Truth**: শুধু `schema.prisma` ফাইলটা মেইনটেইন করলেই হয়, আলাদা করে মডেল ক্লাস বা মাইগ্রেশন কনফিগ ঘাঁটতে হয় না।
- **Less Boilerplate**: কম কোড লিখেই অনেক কমপ্লেক্স কুয়েরি করা যায়।

---

## ৪. বেসিক ওয়ার্কফ্লো (Basic Workflow)

ডেভেলপমেন্টের সময় কীভাবে কাজ করতেন সেটা বলুন:

1.  **Model**: `schema.prisma` ফাইলে নতুন মডেল অ্যাড করা (e.g., `Post` table)।
2.  **Migrate**: টার্মিনালে `npx prisma migrate dev` কমান্ড রান করা। এটি DB-তে টেবিল বানায়।
3.  **Generate**: (অটোমেটিক হয়) Prisma `node_modules`-এর ভেতর নতুন টাইপ জেনারেট করে দেয়।
4.  **Query**: ব্যাকএন্ড কোড-এ `prisma.post.create(...)` ব্যবহার করে ডেটা সেভ করা।

---

## ৫. কিছু অ্যাডভান্সড কনসেপ্ট (Intermediate Knowledge)

### রিলেশনস (Relations)

- **1-1 (One-to-One)**: একজন User-এর একটাই Profile থাকে।
- **1-n (One-to-Many)**: একজন User-এর অনেকগুলো Post থাকতে পারে।
- **m-n (Many-to-Many)**: একটি Post-এর অনেক Category, আর একটি Category-তে অনেক Post।
  - _Implicit m-n_: Prisma নিজেই জয়েন টেবিল হ্যান্ডেল করে।
  - _Explicit m-n_: আপনি নিজে হাতে জয়েন টেবিল বানাবেন (যদি এক্সট্রা ফিল্ড লাগে যেমন `assignedAt`)।

### ইন্ট্রোস্পেকশন (Introspection - `prisma db pull`)

- যদি আপনার **আগে থেকেই ডেটাবেস বানানো থাকে**, তাহলে `schema.prisma` হাতে লেখার দরকার নেই। `prisma db pull` কমান্ড দিলেই পুরো ডেটাবেস স্কিমা কোড-এ কনভার্ট হয়ে যাবে।

### Raw SQL

- Prisma-তেও দরকার পড়লে **Raw SQL** লেখা যায় (যদি কুয়েরি খুব কমপ্লেক্স হয় বা পারফরম্যান্স ইস্যু থাকে) `$queryRaw` ব্যবহার করে।

---

## ৬. সাধারণ ইন্টারভিউ প্রশ্নসমূহ (Common Questions) ❓

**প্রশ্ন: Prisma vs TypeORM/Sequelize - পার্থক্য কি?**

- **Prisma**: `schema.prisma` ফাইলটা মেইন সোর্স। মডেল থেকে ক্লায়েন্ট জেনারেট হয়। রানটাইম এরর কম হয় কারণ বিল্ড টাইম-এই টাইপ চেক করে।
- **TypeORM/Sequelize**: Class বা Decorator ব্যবহার করে মডেল বানানো হয়। বেশিরভাগ এরর কোড রান করার পর বোঝা যায়।

**প্রশ্ন: N+1 প্রবলেম কি এবং Prisma এটি কীভাবে সলভ করে?**

- _সংজ্ঞা_: কুয়েরি করার সময় যদি প্রথমে লিস্ট আনা হয় (১টি কুয়েরি) আর তারপর প্রত্যেকের জন্য আলাদা কুয়েরি করা হয় (N-টি কুয়েরি), টোটাল N+1 কুয়েরি হয় যা স্লো।
- _Prisma সলিউশন_: Prisma ইন্টারনালি "DataLoader" প্যাটার্ন ব্যবহার করে কুয়েরি **ব্যাচ (batch)** করে ফেলে। তাই `findMany`-এর ভেতর `include` ব্যবহার করলে এটি এফিশিয়েন্ট হয়।

**প্রশ্ন: কোন কোন ডেটাবেস সাপোর্ট করে?**

- PostgreSQL, MySQL, SQLite, SQL Server, MongoDB (NoSQL), এবং CockroachDB।

---

## ৭. কুইক কোড স্নিপেট (Code Snippet)

সিনট্যাক্সটা মনে রাখার জন্য:

```typescript
// schema.prisma
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  posts Post[]
}

model Post {
  id        Int  @id @default(autoincrement())
  title     String
  authorId  Int
  author    User @relation(fields: [authorId], references: [id])
}

// app.ts
const newUser = await prisma.user.create({
  data: {
    email: "alice@prisma.io",
    posts: {
      create: { title: "Hello World" }, // একবারে User আর Post ক্রিয়েট!
    },
  },
})
```
